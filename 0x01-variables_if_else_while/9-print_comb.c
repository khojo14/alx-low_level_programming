#include <stdio.h>
/**
 * main - prints lower case alphabets in reverse order
 *
 * Return: (0) Success
 */

int main(void)
{
	int n;

	n = 0;
	while (n < 10)
	{
		putchar(('n') + 0);
		if (n < 10)
		{
			putchar(',');
			putchar(' ');
		}
		n++;
	}

	return (0);
}
