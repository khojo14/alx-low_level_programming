#include <stdio.h>
/**
 * main - prints alphabets in lower case
 *
 * Return: (0) Success
 */

int main(void)
{
	char  n;

	for (n = '1'; n <= '9'; n++)
	{
		putchar(n);
	}
	putchar('\n');

	return (0);
}
